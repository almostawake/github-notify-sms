{
  "name": "github-notify-sms",
  "version": "1.0.0",
  "description": "Poll a public repo for changes and send and SMS notification if it changes, using Twilio.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "typescript": "^5.3.3"
  },
  "dependencies": {
    "@google-cloud/firestore": "^7.1.0",
    "@google-cloud/functions-framework": "^3.3.0"
  }
}
